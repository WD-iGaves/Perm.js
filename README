仅供参考。
...
不要回调函数。
要事件传递。
...
class.js,定义了js中常用的类实现，可以用来实现inherit mixin，instance等实现，有单例实现。

selector.js,选择器用了sizzle.js,但是dom封装了自己扩展的接口，部分样式实现非完全，比如get_style css等，没有:after等fix或者其余，暂时做了Display缓存

enumberable.js 通用的循环器

eventdispatcher.js 自定义事件模块，可被mixin。

cvs.js 一套页面逻辑机制，可进行单独页面内业务分层。带有路由机制。

plan/ 写的一个Todolist,用cvs和框架，但是没有用到路由。

有事找我
http://t.qq.com/igaves 莫大艺术家

此框架千年不更新，参考思路，不要看具体实现。

2012-11-19 log:
	添加了瀑布流插件。可独立使用。
